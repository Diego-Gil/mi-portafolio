//Tu presentación principal.
import { motion } from 'framer-motion';

const Hero = () => {
  return (
    <section className="relative h-screen flex flex-col items-center justify-center text-center px-6">
      {/* Pega aquí todo el código de la sección Hero que tenemos en App.jsx */}
    </section>
  );
};

export default Hero;